import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    isOpen:false
}

const CheckoutSlice =() =>{
    name: 'checkout',
    initialState,
    reducers: {}
}

export default CheckoutSlice.reducers